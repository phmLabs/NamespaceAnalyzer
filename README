Namespace Analyzer
==================

This tool will check if all used (T_USE) namespaces are needed in a given PHP file.

Example:

php bin/console analyze example/unusednamespace.php

find . -name "*.php" | xargs -n 1 php php bin/console analyze $a